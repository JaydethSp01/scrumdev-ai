# Deps presentes en el venv local pero ausentes del poetry.lock (instaladas a
# mano). Se instalan en la imagen para que cloud funcione igual que local:
# pypdf/python-docx -> extracción de documentos (/intake/document)
# requests/urllib3/charset-normalizer -> HTTP utils
# lxml -> parsing; aiokafka -> event bus; certifi -> TLS de httpx
certifi
charset-normalizer==3.4.7
deprecated==1.3.1
importlib-metadata==8.4.0
lxml==6.1.1
pypdf==6.12.2
python-docx==1.2.0
requests==2.34.2
urllib3==2.7.0
zipp==4.1.0
aiokafka==0.14.0
packaging
regex
filelock
fsspec
# Claude Code SDK (combo EXACTO que funciona; el CLI npm @anthropic-ai/claude-code
# lo spawnea con CLAUDE_CODE_OAUTH_TOKEN headless -> genera con tu plan, sin API).
# mcp/starlette/fastapi/anyio PINEADOS: el sdk subía starlette y rompía el boot
# (Router.__init__ got on_startup). Se instala al final -> gana sobre el lock.
claude-agent-sdk==0.1.81
mcp==1.27.1
fastapi==0.115.14
starlette==0.46.2
anyio==4.13.0
