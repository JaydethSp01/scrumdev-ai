# Deps presentes en el venv local pero ausentes del poetry.lock (instaladas a
# mano). Se instalan en la imagen para que cloud funcione igual que local:
# pypdf/python-docx -> extracción de documentos (/intake/document)
# requests/urllib3/charset-normalizer -> HTTP utils
# lxml -> parsing; aiokafka -> event bus; certifi -> TLS de httpx
certifi
charset-normalizer==3.4.7
deprecated==1.3.1
importlib-metadata==8.4.0
lxml==6.1.1
pypdf==6.12.2
python-docx==1.2.0
requests==2.34.2
urllib3==2.7.0
zipp==4.1.0
aiokafka==0.14.0
